{
    "configurations": [
        {
            "type": "pwa-node",
            "name": "Run Current File",
            "request": "launch",
            "program": "${relativeFile}",
            "cwd": "${workspaceFolder}"
        }
    ]
}