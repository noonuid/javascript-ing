import { describe, expect, test } from '@jest/globals';

describe("relational", () => {
    test("example relational", () => {
        console.log(`0 == 1 is ${0 == 1}`);
        console.log(`0 != 1 is ${0 != 1}`);
        console.log(`0 > 1 is ${0 > 1}`);
        console.log(`0 < 1 is ${0 < 1}`);
        console.log(`0 <= 1 is ${0 <= 1}`);
        console.log(`0 >= 1 is ${0 >= 1}`);
    });
});